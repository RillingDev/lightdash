/* import hasKey from "../has/key";
import {
    IGenericObject,
} from "../types";
import objMap from "./map"; */

/* const objMerge = (obj1: IGenericObject, obj2: object): object => objMap(obj1,
    (val, key) => isNil(obj[key]) ? val : obj[key]);

export default objDefaults; */
