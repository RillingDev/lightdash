/**
 * Returns the median of an array of numbers.
 *
 * @function numMedian
 * @memberof Number
 * @since 5.0.0
 * @param {number[]} arr
 * @returns {number}
 * @example
 * numMedian([1, 2.5, 3.3])
 * // => 2.5
 *
 * numMedian([1, 2, 4, 5])
 * // => 3
 */
declare const numMedian: (arr: number[]) => number;
export { numMedian };
